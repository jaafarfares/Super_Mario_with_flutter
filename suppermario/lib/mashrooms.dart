import 'package:flutter/material.dart';

class mymashroom extends StatelessWidget {
  const mymashroom({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 25,
      width: 25,
      child: Image.asset('images/mashroom.png'),
    );
  }
}
